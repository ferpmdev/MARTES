const arrayCuentas = [
  {
    titular: "Arlene Barr",
    estaHabilitada: false,
    saldo: "$3,253.40",
    edadTitular: 33,
    tipoCuenta: "sueldo",
  },
  {
    titular: "Roslyn Torres",
    estaHabilitada: false,
    saldo: "$3,229.45",
    edadTitular: 22,
    tipoCuenta: "sueldo",
  },
  {
    titular: "Cleo Lopez",
    estaHabilitada: true,
    saldo: "$1,360.19",
    edadTitular: 34,
    tipoCuenta: "corriente",
  },
  {
    titular: "Daniel Malone",
    estaHabilitada: false,
    saldo: "$3,627.12",
    edadTitular: 30,
    tipoCuenta: "sueldo",
  },
  {
    titular: "Ethel Leon",
    estaHabilitada: true,
    saldo: "$1,616.52",
    edadTitular: 34,
    tipoCuenta: "sueldo",
  },
  {
    titular: "Harding Mitchell",
    estaHabilitada: true,
    saldo: "$1,408.68",
    edadTitular: 25,
    tipoCuenta: "corriente",
  },
];

// Obtener un nuevo array de cuentas cuyas edades sean menores a 30.

const menoresDeTreinta = (arr) => {
  cuentasMenoresDeTreinta = [];
  for (let index = 0; index < arr.length; index++) {
    const element = arr[index];
    element.edadTitular < 30 && cuentasMenoresDeTreinta.push(element);
  }
  return cuentasMenoresDeTreinta;
};

// console.log(menoresDeTreinta(arrayCuentas));


// Obtener un nuevo array de cuentas cuyas edades sean mayor o igual a 30.

const treintaOMas = (arr) => {
  cuentasTreintaOMas = [];
  for (let index = 0; index < arr.length; index++) {
    const element = arr[index];
    element.edadTitular >= 30 && cuentasTreintaOMas.push(element);
  }
  return cuentasTreintaOMas;
};

// console.log(treintaOMas(arrayCuentas));


// Obtener un nuevo array de cuentas cuyas edades sean igual a 30.
const getTreinta = (arr) => {
  cuentasTreinta = [];
  for (let index = 0; index < arr.length; index++) {
    const element = arr[index];
    element.edadTitular === 30 && cuentasTreinta.push(element);
  }
  return cuentasTreinta;
};

// console.log(getTreinta(arrayCuentas));


// Obtener la cuenta con el titular de la misma más joven.

const getYoung = (arr) => {
  let moreYoung = [];
  let younger = arr[0].edadTitular;
  for (let index = 0; index < arr.length; index++) {
    const element = arr[index].edadTitular;
    if (younger >= element) {
      younger = element;
      moreYoung.push(arr[index]);
    }
  }
  console.log(moreYoung.pop());
};

// getYoung(arrayCuentas);


// Obtener un nuevo array por cada tipo de cuenta.

const byTypeCount = (array) => {
  let corrientCount = [];
  let salaryCount = [];
  for (let index = 0; index < array.length; index++) {
    const element = array[index].tipoCuenta;
    element === "sueldo"
      ? salaryCount.push(array[index])
      : corrientCount.push(array[index]);
  }
  console.table(salaryCount);
  console.table(corrientCount);
};

// byTypeCount(arrayCuentas);


// Obtener un nuevo array con las cuentas habilitadas.

const getStatusCount = (array) => {
  let okCount = [];
  for (let index = 0; index < array.length; index++) {
    const element = array[index].estaHabilitada;
    element && okCount.push(array[index]);
  }
  console.table(okCount);
};
// getStatusCount(arrayCuentas);

// Obtener un nuevo array con las cuentas deshabilitadas.
const getBadCount = (array) => {
  let badCount = [];
  for (let index = 0; index < array.length; index++) {
    const element = array[index].estaHabilitada;
    !element && badCount.push(array[index]);
  }
  console.table(badCount);
};
getBadCount(arrayCuentas);

// Obtener la cuenta con el menor saldo.

// Obtener la cuenta con el mayor saldo.
